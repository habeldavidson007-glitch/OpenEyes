{
  "domain": "finance",
  "version": "0.2",
  "decay_rate_per_year": 8,
  "max_fragment_age_years": 2,
  "rules": [
    {
      "id": "FIN-001",
      "name": "Require Source URL",
      "description": "Every finance fragment must have a source_url",
      "check_type": "requires_field",
      "config": { "field": "source_url" }
    },
    {
      "id": "FIN-002",
      "name": "Require Year",
      "description": "Every finance fragment must have a year",
      "check_type": "requires_field",
      "config": { "field": "year" }
    },
    {
      "id": "FIN-003",
      "name": "No Stale Fragments",
      "description": "Fragments older than 2 years cannot exceed credibility score of 60",
      "check_type": "recency_cap",
      "config": { "max_age_years": 2, "score_cap": 60 }
    },
    {
      "id": "FIN-004",
      "name": "No Price Predictions",
      "description": "Any fragment containing price targets must include uncertainty note",
      "check_type": "requires_uncertainty_note",
      "config": { "trigger_tags": ["price_target", "prediction", "forecast"] }
    },
    {
      "id": "FIN-005",
      "name": "Minimum Credibility",
      "description": "Finance fragments must have recognized credibility class",
      "check_type": "minimum_credibility_class",
      "config": { 
        "allowed": [
          "international_institution",
          "academic_research", 
          "government_agency",
          "financial_news",
          "financial_publication",
          "financial_education",
          "regulatory_filing",
          "central_bank",
          "exchange_data"
        ] 
      }
    }
  ]
}
